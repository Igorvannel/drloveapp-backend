server:
  port: 4044

spring:
  application:
    name: drlove-app
  datasource:
    url: jdbc:mysql://localhost:3306/drloveappdb?createDatabaseIfNotExist=true
    username: igor
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
